<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 普通对话框 -->
    <style name="alert_dialog" mce_bogus="1" parent="android:Animation">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowEnterAnimation">@anim/dialog_scale_enter_default</item>
        <item name="android:windowExitAnimation">@anim/dialog_scale_exit_default</item>
    </style>

    <style name="alert_dialog_no_bg" parent="alert_dialog">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- 弹窗Activity -->
    <style name="dialog_activity" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
